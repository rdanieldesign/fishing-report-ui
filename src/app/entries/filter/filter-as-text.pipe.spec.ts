import { FilterAsTextPipe } from './filter-as-text.pipe';

describe('FilterAsTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAsTextPipe();
    expect(pipe).toBeTruthy();
  });
});
