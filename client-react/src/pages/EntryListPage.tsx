// Placeholder — serves /entries, /my-entries, /users/:userId/entries, /locations/:locationId/entries
// Full polymorphic implementation in Phase 4
export function EntryListPage() {
  return <div className="p-8">Entry List Page</div>;
}
